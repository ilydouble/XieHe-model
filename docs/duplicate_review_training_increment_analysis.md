# 284组重复标注人工核对结果增量分析

日期：2026-08-12
人工结果：`/Users/liruirui/Downloads/重复标注核对结果_1786519679959.csv`

## 结论

人工结果覆盖全部284组重复影像：选择图1为114组、选择图2为149组、两个都不直接选为21组。283组有2个候选，1组有3个候选。

这份结果可以进一步扩充两个训练数据集，但必须按六点与椎体角点任务分别处理。21个`neither`组中有5组明确指定两个任务使用不同版本；其余备注包含需要局部修正、特殊解剖、不在标注范围或没有可直接使用版本，不能整份JSON机械导入。

| 任务 | 可净新增 | 当前已含所选版本 | 需替换现有标签 | 当前已含但人工否决 | 人工未选且未入库 | 所选标签不完整/异常 |
|---|---:|---:|---:|---:|---:|---:|
| 六点 | 263 | 1 | 0 | 2 | 14 | 4 |
| 角点 | 247 | 16 | 1 | 0 | 16 | 4 |

六点263份与角点247份中有244幅共同影像；另外六点独有19幅、角点独有3幅，候选图像并集为266幅。每个任务内部候选图像SHA-256均唯一，拟使用的`eap_`目标文件名冲突为0。

## 预计规模

若按本报告应用：

- 六点`pose_data`：当前1494份，新增263份并移出2份人工否决的既有代表，最终1755份；train由1195变为1456，val 149和test 150保持不变。
- 角点`pose_corner_data`：当前2252份，新增247份并替换1份现有标签，最终2499份；train由1976变为2223，val 137和test 139保持不变。

## 需清理或替换的既有样本

六点应移出两个此前自动选择、但本次人工结果为`neither`的代表：

- G0090：现有同图代表属于人工未选择可用版本；
- G0202：备注“比较特殊，T只有11个”，人工未选择可直接使用版本。

角点应替换一份同图标签：

- G0247：人工选择恢复备份中的`532_SCO1911P0036_20191112_label.json`；当前数据中同图为`eap_355_SCO1911P0036_20191112.txt`，两者标签不一致。图像无需重复加入，只替换标签并建立原标签备份。

## 保留排除项

- 六点4组所选版本仍存在缺点、越界或结构异常，不能直接导入；
- 角点4组所选版本缺少1至2节椎体的4角点，不能直接导入；
- 对备注要求局部拼接或修点、但没有明确可直接使用完整版本的组，继续排除；
- 所有候选均匹配`assignment_all.xlsx`正式患者范围，本轮无白名单外候选。

分析门槛为：人工已选、任务点位完整、坐标及几何合法、六点左右符合当前统一规范、属于`assignment_all`，且当前对应任务数据集中不存在同内容图像。

## 应用结果

用户确认本报告后，已于2026-08-12正式应用：

- 六点新增263份，G0090/G0202两份人工否决代表移入可恢复隔离目录；最终1755份（train 1456、val 149、test 150）。
- 角点新增247份，G0247原标签备份后替换为人工所选版本；最终2499份（train 2223、val 137、test 139）。
- 两套数据全量图像解码、配对、标签结构、精确重复和跨split精确重复检查均通过，问题为0。

正式逐文件清单位于`datasets/import_records/duplicate_review_20260812/import_manifest.json`，隔离与替换前备份位于`datasets/quarantine_duplicate_review_20260812/`。
