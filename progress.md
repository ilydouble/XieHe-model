# 训练导出数据审计进度

## 2026-08-22：六点纵向系统偏差与Pose/Detection选择

- 复用最新模型175张test的逐点原始预测，新增有符号dy和上下跨度统计，确认肩点偏高、下方点偏低是真实的小幅系统偏差，而非个别视觉印象。
- 按来源拆分后，偏差主要由98张 `eap_` 新人工子集驱动；旧来源77张的纵向偏差明显更小。
- 核对Ultralytics的 `xy/xyn` 输出、标签槽位、训练YAML、最新权重路径、比较脚本和部署权重，未发现能解释该方向偏差的letterbox或归一化BUG；但发现比较脚本和部署目录仍默认加载2026-05-11旧Pose权重。
- 核对Detection现状：活动派生数据集不存在，现有权重早于本轮扩充，历史标签又是Pose真值的机械派生，不能作为公平的任务优劣证据。
- 结论为继续使用Pose，先统一标注规范并做低增强Pose对照；Detection只作为同数据同split的受控A/B，不作为当前偏差的直接修复。本轮未修改训练数据、训练代码或用户正在编辑的比较脚本。

## 2026-08-13：解释"标注看着还行但预测误差很大"

- 用户反馈标注抽查没问题，但训练出的模型预测误差很大；先复核`datasets/pose_data`结构完整性（0缺失、0越界、0左右顺序违反），排除数据损坏或缺标注导致误差。
- 复算175份人工核验包manifest后发现：`eap_`98张test图原图对角线中位数约6037px、`old`77张仅3811px，相差1.58倍；同样比例误差在eap_高分辨率图上换算出的绝对像素数天然更大，这是此前"41.4px平均误差"给人直觉偏大的主因之一，与标注或模型质量无关。
- 按对角线归一化后的真实指标其实健康：点召回100%、PCK@2%对角线92.67%、eap_/old平均误差分别约占对角线1.09%/0.72%。
- 核实训练/推理均固定`imgsz=800`（`best_performance-3/args.yaml`），高分辨率eap_原图压缩倍数远大于old，进一步放大了绝对像素误差的观感；但eap_内部按分辨率四分位比较误差与分辨率无正相关（Pearson -0.10），说明不是分辨率越高误差越离谱。
- 确认`flip_idx`已于该次训练权重生成前提交，`fliplr=0.5`增强在此次训练中已正确生效，排除此前担心的翻转增强失效历史问题。
- 剔除尺度效应后仍有约50%的真实差距（eap_对角线归一化误差比old高约50%，六点全部同向），与此前定位的eap_取点更靠外侧、旧标注更靠内侧的标注风格差异一致，是唯一需要处理的真实成因。结论和建议已写入findings.md；本轮未修改训练数据或模型权重。

## 2026-08-13：最新六点 Pose 模型人工核验包

- 已确认最新模型、旧 Detection 模型时间差、175 份 test 集和 E 盘挂载路径。
- 已决定核验包同时包含预测、真值和叠加视图，并输出逐点误差与漏点统计。
- 已实现 `scripts/build_six_point_model_review.py` 和 2 项回归测试；测试、py_compile 与 diff 检查通过。
- 已用最新模型完成175张test全量推理并生成180个核验包文件，大小约60 MB；1050/1050点检出，PCK@图像对角线2%为92.67%。
- 已直接查看最差、中位和最好三张预览，真值/预测/叠加绘制正确。内置浏览器因安全策略拒绝本地`file://`页面，未绕过限制，将以静态检查补足页面验收。
- 页面脚本语法、175行索引、175张预览解码、人工判断控件、自动保存及导出结构检查通过。
- 已复制到E盘指定目录，180个正式文件逐文件SHA-256一致；限定清除新包内181个AppleDouble旁车后再次复验通过。
- 本轮工具、测试与记录已提交；阶段119–123全部完成。
- 用户反馈CL点误差偏大且不理解原因；已用最新权重对175张test重新逐图推理并落盘每点真值/预测/误差原始数据，排除左右点互换（最差30例预测点均离CL真值最近而非其他点）。
- 按`eap_`/旧数据来源拆分统计确认差距主因：CL在`eap_`子集误差60.9px、旧子集31.1px；在train集随机120份复核出现同一模式（eap 61.3px vs old 33.4px），说明是训练数据里稳定存在的来源相关系统偏差，不是test集抽样巧合。
- dx方向直方图显示`eap_`子集的CL预测系统性偏负（更靠左/靠锁骨外侧），旧子集大致对称；裁剪叠加图确认预测点和真值点都落在左锁骨骨影上，只是沿骨干取点位置不同，不是检测到无关结构。
- 诊断结论与建议已写入findings.md；本轮未修改任何训练数据或模型权重。

## 2026-08-12：当前训练数据复盘

- 用户要求重新分析 `datasets` 下当前训练数据，包括现有规模、标签质量、原有数量和实际加入数量。
- 本轮只读核查活动训练目录，并以导入清单、隔离清单和当前文件三方交叉验证历史增量；不修改任何训练图像或标签。
- 初步实数：六点为533/77/75共685对；椎体角点为1465/74/75共1614对。
- 用户将优先级改为先导入新六点数据。旧六点685份已统一为画面左侧CL/IL/SL，因此新数据使用`swap_pairs`转换标签槽位，不镜像图像；继续以assignment_all.xlsx作为正式人工标注白名单。
- 首次沿用`swap_pairs`执行时，脚本识别到源JSON本身已经统一，安全跳过812份且未改动数据；随后新增`preserve`策略并以单元测试确认原样保留标签身份。
- 正式导入结果：809份加入train；1份混合左右关系和2份不在assignment白名单的测试数据跳过。六点集从685对增至1494对，分区1342/77/75。
- 导入后全量审计：1494份均为合法六点Pose格式，三组均满足CL<CR、IL<IR、SL<SR；精确重复组0，跨split精确重复组0，自动结构告警0。
- 两套活动数据3108张图已完成全像素解码、SHA-256、配对和标签逐字段复核；损坏图0、任务内重复0、跨split重复0。
- 角点新增871份均为18类完整标签；旧743份仍缺V0，导致当前val/test无V0。旧角点bbox与四角点外接范围存在系统性小偏差，新增角点bbox全部精确。
- 已抽查新增数据最小分辨率和最宽画幅代表图，仍是有效正位全脊柱片；准备更新最终导入报告并给出训练建议。
- 已更新`docs/e_drive_training_data_import_report_20260812.md`：记录最终数量、两套标签全量审计、旧角点bbox差异、V0评估缺口、患者级划分风险和实际训练配置核对。
- 当前结论：六点可直接训练；新增角点可直接训练；旧角点四点本身正常但bbox建议另行统一；当前val/test不包含新增来源，不能完整评价新增数据域。

## 2026-08-12：六点数据重新划分

- 用户要求解决当前1342/77/75分区中val/test偏少且没有新增来源的问题。
- 采用约8:1:1目标；保留现有旧val/test，再按assignment患者ID整组移动新增样本，防止同一新增患者跨split。
- 已新增可复现重分脚本和2项测试；预演精确选择43个val患者/72份、43个test患者/75份。
- 正式重分后为1195/149/150，来源构成为train旧533+新662、val旧77+新72、test旧75+新75。
- 全量验证通过：1494张均可解码、图像标签配对正确、结构与左右告警0、精确重复0、跨split重复0、新增患者跨split泄漏0。

## 2026-08-12：旧六点训练标签左右语义统一

- 用户确认采用XieHe-System前端领域语义作为统一规范，开始修正当前`datasets/pose_data`旧六点训练标签。
- 本次只交换三对关键点槽位`CR/CL`、`IR/IL`、`SR/SL`；不镜像图像，不修改bbox、split或坐标数值，并先建立逐文件哈希备份。
- 当前活动集为685份（train 533、val 77、test 75），全部是单行23列且全部为旧约定`CR<CL、IR<IL、SR<SL`，不存在混合方向样本。
- 已新增可预演、可备份、可幂等执行的修正脚本及测试，并将训练YAML补充`flip_idx: [1,0,3,2,5,4]`；同时把E盘导入器目标方向改为新规范。
- 默认Python运行E盘导入器测试时缺少Pillow；未安装依赖，按仓库既有做法改用`/opt/miniconda3/envs/cv/bin/python`复测。
- 已正式备份并修正685份活动Pose标签；逐份断言bbox前5列不变，六个关键点仅按`[1,0,3,2,5,4]`置换，图像树SHA-256前后均为`8cadff...f37b4`。
- 修正后685份全部满足`CL<CR、IL<IR、SL<SR`；专项审计得到685份六点Pose、0个问题，重复执行修正脚本显示0份待修改。
- 已同步75份仍有对应Pose的派生Detection标签并备份；83份没有当前Pose的孤立旧Detection标签内容摘要前后相同，未修改也未删除。最终Pose→Detection为75份精确一致、0份不一致。
- 修正备份位于`datasets/pose_data_lr_backup_20260812/`，包含685份Pose原标签、75份Detection原标签及逐文件SHA-256清单。
- 已新增`docs/pose_lr_normalization_report_20260812.md`记录规范、执行统计、验证摘要、Detection同步和XieHe-System部署注意事项；阶段59–62完成。

## 2026-08-11

- 已读取 `planning-with-files-zh` 技能说明。
- 已检查工作区状态；发现与本任务无关的既有改动，将保持不动。
- 已创建本次任务的计划、发现和进度文件。
- 已盘点导出目录：单层、3246 个文件、约 2.8 GiB，主要为 PNG 与 `_label.json` 文件。
- 已确认当前仓库与源导出项目的本地规则；本任务只在 Model 仓库新增审计工具。
- 已抽样标注结构并检索现有脚本；现有工具主要面向 YOLO TXT，不适用于本次 JSON 导出，因此需要新增独立审计脚本。
- 已完成首轮全量 SHA-256 与原始文件名重复扫描，确认存在真实内容重复；下一步缩减输出并关联比较标注。
- 已取得重复汇总、配对统计和标签分布；阶段 1–2 完成，进入脚本实现。
- 已新增 `scripts/audit_training_export.py`、对应单元测试与使用说明。
- 单元测试 4/4 通过；`py_compile` 与 `git diff --check` 通过。
- 已完成真实数据首轮运行；发现多数 `.png` 可能是 JPEG 内容，正在修正格式识别后复跑。
- 已增加 PNG/JPEG 魔数识别与重复标注冲突细分，单元测试增至 5 个并全部通过。
- 已完成真实数据最终审计：10 个 error、549 个 warning；完整报告写入 `/private/tmp/training_export_audit.json`。
- 交付前检查通过：5/5 单元测试、`py_compile`、`git diff --check`；本机未安装 Ruff，已记录为跳过。

## 2026-08-11：旧训练集与新增数据选择

- 用户指出旧训练集标注同样未经确认；开始对旧训练集和新导出数据做同标准比较，不默认任一来源为真值。
- 已盘点旧数据集规模，初步确定椎体四角增量训练的主要基线为 `datasets/pose_corner_data`（743 对）。
- 已确认旧 YOLO 四角标签结构，并完成旧/新图像精确哈希比较：交集为 0，下一步检查近似重复与旧标签质量。
- 已完成旧标签结构审计：742 份含 17 个类别，1 份含 16 个；class 0/C7 在旧训练集中完全缺失，其余结构坐标合法。
- 已定位可用的 Pillow/NumPy 运行时，将用于旧/新图像近似重复比较。
- 感知指纹全量计算两次超过单次等待窗口；输出会话 ID 未被首轮调用保留，已记录并改用显式会话轮询。
- 用户终止上一轮并明确四项审计目标；已停止“先挑增量训练数据”的扩展，改为分别核实两个模型及四项问题。
- 已核实模型结构：椎体 72 点为 Pose；六点同时有 Pose 与六类 Detect 实验，当前对比脚本倾向比较两者，但示例服务仍加载六点 Pose。
- 已核实 source 语义：拖动点会被标为 manual，未移动点保留 ai；导出不含 AI 初始快照，需重跑对应模型才能量化一致性。
- 已检查训练记录与当前数据目录；发现六点现存 split 与历史训练不一致，历史 mAP 不能在未确认标签和泄漏前直接当准确率。
- 已完成旧/新精确重叠定位：同一张新图同时与旧六点集精确重叠、与旧椎体集感知重叠；两边标注均存在显著差异。
- 已完成新导出按六点/72 点分别统计完整性和重复冲突，并发现旧六点标签存在大量越界/左上聚集异常。
- 已验证三个候选权重可在 `cv` Conda 环境加载，任务类型与用户记忆一致：六点 Detect 候选、椎体角点 Pose。
- 已对 120 张导出图运行三个候选权重：椎体按类别匹配与导出标注高度一致，按纵向重排时受漏检影响明显；两个旧六点候选与导出六点严重不一致。
- 已核实实际 AP 服务使用六点 Pose 与椎体 Pose，六类 Detect 仅为实验候选；线上权重未包含在当前工作副本中。
- 已复算六点回退公式，确认本批 AI 六点并非主要由当前版本的椎体估算逻辑直接生成。
- 已完成 1670 张新图内部及其与两个旧数据集的 512 位感知指纹扫描；除已知 1 张旧/新重叠外没有第二张，内部存在 297 对强近似非字节重复候选。
- 四项审计均已形成结论；未删除、改名、转换或合并任何原始图像、标注与训练数据。
- 用户要求人工复核异常：开始为 15 张椎体角点顺序告警图和 4 张六点左右关系告警图生成原图叠加预览，并核实左右翻转是否会解释告警；暂缓处理 7 个越界点。
- 已核实上传、编辑、批量导入和批量 AI 脚本中的水平翻转入口；翻转可导致该现象，但导出 JSON 不保留翻转历史，必须可视化复核。
- 已生成临时复核包：21 张椎体异常明细图、4 张左右对照图、HTML 和 CSV，准备抽查渲染效果。
- 已抽查角点编号冲突、明显椎体错位、全组六点反向和局部 SL/SR 反向四种代表图；渲染可读，且可视化进一步确认“整图翻转只能解释整组反向，不能解释单独一组反向”。
- 已将复核包归档到 E 盘并验证：椎体预览 21 张、左右对照 4 张、CSV 26 行（表头+25 条）、HTML 校验一致、AppleDouble 数量为 0。本阶段完成，7 个越界点保持未处理。
- 用户完成视觉核对并授权修正规范化数据集：开始准备可恢复备份、水平翻转、无重复四角重排和错误重复样本移除；越界点不在本轮范围。
- 已完成修正脚本干运行及 `/tmp` 副本试执行，镜像、全坐标变换、四角重排、特殊合并和删除目标均通过断言验证；准备创建 E 盘恢复备份并正式应用。
- 已正式应用人工确认修正并建立 32 文件恢复备份：镜像 4 张、重排 7 个椎体、合并 1 组、移除 9 个错误重复样本。
- 首次复审发现镜像图角点编号需同步交换；已补充 `1↔2、3↔4` 并记录修复增补清单，椎体角点顺序告警最终为 0。
- 最终全量审计完成：1661 图、1567 配对 JSON、7 error、513 warning；295 个精确重复组、282 个冲突组。主分析报告和完整审计 JSON 已更新归档，相关 AppleDouble 已清理。
- 用户确认将 94 张无 JSON 图像移出训练工作目录；开始按最终审计清单建立待标注目录和迁移清单。
- 已将 94 张无 JSON 图像移动至 `待标注_94张/`，并生成含 SHA-256 的迁移清单和说明；源目录与目标目录计数验证通过。
- 移动后全量审计完成：1567 图、1567 JSON、1567 对配对、0 张无标注；284 个精确重复组全部存在标注冲突，7 个坐标越界和 2 条人工确认方向例外保持未处理。
- 已把最终审计 JSON 与更新后的主分析报告写回 E 盘并校验 SHA-256 一致；本阶段完成。
- 用户允许六点与脊柱模型使用不同图像；开始建立两个任务独立的可训练、待复核、排除清单及可重复生成脚本。
- 已完成双任务清单生成脚本和 5 项单元测试；`py_compile`、5/5 测试与 `git diff --check` 均通过，进入真实数据生成阶段。
- 已在真实规范化数据上生成两份完整 CSV：六点 812 可训练/671 待复核/84 排除；脊柱 Pose 874 可训练/567 待复核/126 排除。
- 已验证两个 CSV 各 1567 条、标注文件名唯一、所有图像/JSON 存在、可训练样本完整、任一精确重复组至多一个可训练代表；两个可训练池为共同 743、六点独有 69、脊柱独有 131。
- 已将清单、汇总 JSON、规则说明和生成脚本归档到 E 盘；临时目录与归档目录逐文件一致，清单目录 AppleDouble 为 0，现有规范化数据集未被改动。
- 用户要求判断1257是否删除，并为其余越界样本生成可视化；已确认1257应仅从六点任务排除、保留用于脊柱任务，其余复核范围为4份JSON。
- 默认 Python 缺少 Pillow；已记录并改用现有 `cv` Conda 环境，不修改项目依赖。
- 已实现扩展画布越界点可视化脚本及测试；`cv`环境直接运行测试文件1/1通过，`py_compile`通过，准备生成4份真实复核图。
- 已生成4张真实越界点复核图；视觉抽查发现并修复右侧标签裁切，重新生成后显示完整。复核包已加入HTML、CSV、JSON、说明和可复现脚本，准备归档到E盘。
- 已将越界点复核包归档到E盘并验证：4张预览、6条越界点索引、临时/归档逐文件一致、AppleDouble为0；原规范化数据仍为1567图和1567 JSON，1257未删除。
- 收到人工复核结论：532错误，其余越界样本合乎常理；开始按可恢复方式移除532，并复审审计与两个模型清单。
- 已建立532恢复备份（含图像、JSON、SHA清单和人工结论），逐字节验证后从规范化工作数据集移除532；355保留，进入全量复审阶段。
- 已完成移除532后的全量审计：1566图+1566 JSON、6个越界坐标、283个精确重复组，无缺JSON。
- 已重新生成任务独立清单：六点812可训练/671待复核/83排除；脊柱Pose 875可训练/565待复核/126排除。
- 已先备份旧报告、旧审计和旧清单，再更新E盘主报告、审计归档、两份清单及越界点人工确认结果。
- 交付验证通过：两份CSV各1566条且无532，355六点排除/脊柱Pose可训练；所有文件路径存在，临时结果与E盘归档哈希一致，本次AppleDouble旁车已清理。

## 2026-08-11：旧六点数据集专项复核

- 用户要求核查原六点训练集标注问题；已按文件规划技能恢复审计上下文并新增专项阶段。
- 已完成Pose/Detection目录盘点和首轮全量格式检查：确认158份真六点标签、75份误混入的椎体四角标签；真六点无坐标越界，但有83份全部点聚集于左上20%。
- 已确认Detection的158份标签均为Pose六点的精确派生结果，无法用作独立交叉验证；下一步视觉核实左上聚集和新旧左右语义。
- 已视觉抽查唯一train样本和一份正常六点对照样本；确认train图解剖范围完整，左上聚集不是原图裁剪所致，高度疑似归一化尺度错误。
- 已核对历史模型报告和Git跟踪状态：文档中的完整旧集应有1565张，当前副本只有233张；数据目录未被Git跟踪，无法从Git恢复缺失的历史训练分区。
- 已搜索spine工程树和E盘spine_data，未发现完整旧六点数据副本；仓库中也未定位到83份左上聚集标签的原始生成脚本。
- 已完成异常/正常六点批次的文件名、visibility、图像尺寸和坐标中位数对比；确认83份异常是单独旧式批次，且不能用单一系数机械放大修复。
- 已确认75份混入文件与pose_corner_data中的图像和标签全部逐字节一致；另核实2条bbox告警只是1e-6的小数舍入误差，已在审计器中调整为1e-5容差。
- 已完成256位dHash近重复扫描；确认7对同图重编码副本，每对都是椎体四角混入版本与左上错误六点版本，全部位于test。
- 已核对新旧同影像的三组L/R关系，确认新旧标注方向规则全部相反；并确认六点YAML缺少flip_idx，导致训练脚本中的fliplr=0.5被当前Ultralytics自动禁用。
- 已完成可复现审计工具实跑，输出 /private/tmp/旧六点数据集审计_20260811/audit.json、issues.csv和report.md；自动结果为83份左上错误隔离和75份椎体任务混入隔离。
- 已形成 docs/legacy_six_point_dataset_audit.md 专项报告，明确当前可用train=0、val=0，仅有75份test样本通过自动结构检查，且仍需人工复核。旧数据未被修改。
- 最终断言复验通过：158份真六点、75份椎体混入、83份左上聚集、158份Detection精确派生均与报告一致。完成检查脚本因不识别现有`[complete]`语法而误报，已人工确认专项阶段31–34全部完成。

## 2026-08-12：恢复后完整六点数据集复核

- 用户已将旧数据集重新复制到 `datasets/`；开始对恢复后的实际目录重新盘点和全量审计。
- 本轮只读检查图像和标签，不沿用之前233份不完整副本的计数，也不自动修改、删除或迁移数据。
- 已盘点恢复后目录：`pose_data` 为2308对（1846/231/231），其中1565份是真六点Pose、743份是混入的椎体四角Pose；`pose_det_data` 仍只有158对。
- 已全量复跑现有审计器：六点中879份触发左上20%系统性聚集，7个精确重复图像组中3组跨split；158份Detection均为Pose标签的精确派生。
- 现进入分区、坐标批次、重复组标签差异和视觉样本复核，确认自动阈值对应的真实错误范围。
- 已确认879份左上异常和686份其余标签是visibility、坐标范围完全分离的两个批次；抽样叠加图确认879份均为完整原图上的错误坐标。
- 已增强审计器，新增分区标签类型、自动结构通过数、三组左右顺序、髂骨/骶骨纵向关系和重复图标签冲突统计；4/4单元测试、py_compile与diff检查通过。
- 在剩余686份中定位并可视化确认3份结构异常；当前683份通过自动结构规则，但仍需人工抽查点位精度。
- 已重写 `docs/legacy_six_point_dataset_audit.md`，以恢复后的2308对实际数据替代此前不完整副本报告。
- 最终复验通过：1565份六点、743份四角混入、879份左上错误、3份结构待复核、683份自动结构通过；数据集文件保持未修改。
- 用户再次询问左上聚集是否代表标签有问题；已重新运行完整审计与4项单元测试，结果与报告完全一致：1565份六点中879份为左上系统性错误，另有743份椎体四角任务混入、3份结构异常、683份自动结构通过。再次查看跨train/val/test的异常/正常叠加图，确认异常原图覆盖完整解剖范围，问题来自标签坐标而非图像裁剪。
- 用户清理数据后再次全量复核：`pose_data`现为534/77/75共686对，全部为六点格式，左上错误、四角混入、精确重复和跨split重复均归零。自动结构通过683份，仍保留原先3份结构异常；4项审计器单元测试通过。
- 补充执行686张图像严格解码检查，发现train中1张PNG的IDAT CRC损坏，Pillow和OpenCV均无法完整解码；当前未找到同名完好副本。
- 对685张可解码图像完成256位dHash近重复复核，距离≤4的候选为0；核对3份异常标签原始坐标与既有叠加图，确认异常仍未修正。当前建议训练候选为682份（532/76/74）。
- 用户授权修正4份异常后，已解析损坏PNG结构，确认其压缩流与尾部数据实质缺失，不能安全修复像素；已建立5文件原始备份并逐项通过SHA-256一致性验证，准备修正3份标签并隔离损坏图像对。
- 已重排3份Pose标签并把损坏PNG及标签移至可恢复隔离目录；首轮全量复审为685对、解码失败0、Pose结构告警0。发现与派生Detection数据有1份不一致，进入同步核对。
- 已备份并同步更新test异常样本的派生Detection标签。最终审计为685份六点Pose、问题0、Pose→Detection转换不一致0；置换、bbox不变、隔离文件哈希、全图解码及4项单元测试全部通过。
- 已生成并人工查看三份修正标签叠加图，左右身份、髂骨与S1点组位置均恢复合理。备份README/manifest已写入；阶段45–47完成。

## 2026-08-12：284组重复标注人工核对包

- 用户要求把同时存在六点差异和脊柱点差异的284个精确重复组制作成可交互核对包，页面支持选择图1、图2或两个都不对。
- 将先核实284组对应移除532前的有标注快照；若当前规范化目录只剩283组，则从已建立的532恢复备份补齐该历史组，不修改当前工作数据集。
- 已核实历史审计与当前审计：284组确属移除532前快照，当前数据为283组；532图像和JSON均在恢复备份，可与现有355补齐历史组。
- 284组涉及569张图，其中283组为双成员、1组为三成员；交互页面将对双成员提供图1/图2/都不对，对三成员增加图3选项。
- 已实现核对包生成器和单元测试，支持历史审计、恢复备份、缩略底图、六点/脊柱叠加开关、三选一、备注、本地自动保存、筛选、快捷键、JSON/CSV导入导出。
- 首次真实生成得到284组、569份候选和284张底图；验证时发现3组六点为缺失结构差异而非共同坐标差，已修正指标逻辑和页面措辞，避免显示为“六点无差异”。
- 使用本地浏览器实际打开页面，确认双图标注叠加清晰、284组进度显示正确、选择图1后自动进入下一组且进度变为1/284；测试中发现跳转框Enter交互缺失，已补充修正和测试。
- 修复后浏览器复验通过：输入123并回车可进入唯一三候选组G0123并显示图3；G0024正确显示六点和脊柱均有结构差异；备注跨组返回后仍恢复。标准Blob导出按钮存在，但内置测试浏览器未暴露下载事件，未重复触发。
- 用户进一步要求去重、复审并分别合入两套训练数据。已确认核对包尚无任何人工选择结果；将先完成核对包验证与E盘归档，选择结果作为实际去重和合并的前置条件。
- 核对包验证通过并已归档到E盘：284组、284张可解码预览、284行未选择初始结果、必需页面文件齐全；2项测试和py_compile通过。
- 首次归档产生290个AppleDouble旁车，已限定在核对包目录内清除；归档与临时包逐文件一致，活动数据未修改。阶段42–44完成，等待用户导出人工选择结果后继续阶段48–51。

## 2026-08-12：从E盘构建YOLO增量训练数据

- 用户澄清目标是以脚本从E盘规范化数据按任务去重、筛选和转换后追加到仓库两个YOLO数据集，而不是等待284组逐一人工裁决。
- 已实现`import_e_drive_training_data.py`及测试：支持默认预演、`--apply`实际导入、任务独立清单、源图严格解码、JSON尺寸核对、YOLO转换、全目标split内容哈希排重、只追加train、目标冲突检测、幂等重跑和机器清单/Markdown报告。
- 真实dry-run完成：六点812份与角点875份候选全部通过完整像素解码、尺寸核对、标签转换和目标全split内容哈希排重，均为planned；候选并集944张，无目标重复或重名冲突。
- 量化确认左右约定冲突：现有六点685/685遵循CR<CL等目标关系；新候选811份完全相反、1份混合。脚本增加默认阻断、交换三对标签、镜像图像三种显式策略，并可用`--tasks`只导入角点。
- 导入脚本回归测试扩展为3项；3/3、py_compile与diff检查通过。下一步先实际导入不受冲突影响的875份角点数据，并等待六点统一策略确认。
- 已实际导入875份椎体角点样本：`pose_corner_data`现为train1469、val74、test75，共1618对；既有val/test未修改。
- 合并后全量图像解码、配对、18类/4点格式、坐标、visibility、精确重复和跨split复核通过，问题为0。首轮复核曾把visibility=2误作坐标范围检查，修正规则后确认是检查器误报而非数据错误。
- 导入器增加混合左右关系保护并扩展为4项测试；六点`swap_pairs`真实预演为811 planned、1 skipped，混合样本不会被整批规则误处理。
- 用户补充关键信息：只有E盘根目录的`assignment_all.xlsx`记录正式有效的人工标注，其他数据属于测试。已立即停止六点导入，并暂停沿用旧任务清单结论；先解析Excel与已导入875份角点的交集，再做可恢复更正。
- 已按表格读取流程确认Excel含815行正式分配、815个唯一患者ID；以Patient_short_id完整子串匹配后，角点875份中871份正式、4份测试，六点812份中810份正式。
- 导入器增加`--assignment-xlsx`白名单过滤并通过5项测试；另新增可恢复隔离脚本及1项测试。4对误导入角点文件已按SHA移入`datasets/quarantine_non_assignment_20260812/`。
- 更正后角点集为1465/74/75共1614对，新增正式样本871份；全量解码、配对、18类4点格式、白名单归属、精确重复和跨split检查问题均为0。
- 已将浏览器复验后的Enter跳转修正版HTML同步到E盘；页面SHA-256与临时包一致，随后清除本次同步产生的唯一AppleDouble旁车。E盘最终断言仍为284组、569候选、284张预览、284/284双任务冲突。

## 2026-08-12：服务器角点数据下载后重叠复核

- 已确认Downloads中的服务器角点images/labels完整为1108/139/139，共1386对，全部配对和解码正常；标签坐标合法，但存在55个超出18类YAML的class 18/19实例。
- 服务器702份历史CR命名数据与202605的743份按患者号+检查日期交集494；进一步逐对降采样像素比对，482对相关系数>=0.99，可确认为同一影像内容的不同分辨率版本，12对画面不一致。
- 已核对482个同影像对的class 1-17角点标签，0对完全一致，确认它们是同图不同标注版本，不能当作独立训练样本重复加入。
- 已发现服务器集内部5组SHA-256完全重复图像，其中3组跨train/val；4组标签不同、1组标签相同。当前只读分析，未修改下载目录或训练数据。
- 已按影像内容匹配规则保留5个可信版本，把5个冗余图像/标签对移入Downloads可恢复隔离目录；记录文件写入`datasets/import_records/server_corner_dedup_20260812/`。
- 去重后为train 1105、val 137、test 139，共1381对；配对缺失0、活动目录SHA-256重复组0。隔离5图+5标签与manifest哈希全部一致。
- 已完成服务器1381与当前1614的全库图像指纹扫描和高分辨率复核：确认490幅同影像，全部落在旧743中；与新增871的高置信重叠为0。
- 融合时可净新增891份，总量理论上由1614增至2505；490份同图不能重复加入。891份中53份需删除class 18/19行，1份缺class 12，且整批bbox约定与新871份不同。当前只给出融合判断，尚未导入服务器数据。
- 已量化490对重叠标注：共同33316个角点的距离中位数0.0064，63.27%小于等于0.01、80.55%小于等于0.02、2.03%大于0.1，最大0.2649；两版只能称多数接近，不能称标注一致。
- 已追溯743份标签为NRRD分割mask经minAreaRect自动转换的角点，不是pose-corner模型预测输出；现有3D Slicer文件无法证明mask最初是纯人工还是AI初标后人工修订。服务器版本也来自seg_data自动转换，而非角点模型预测。
- 已实现并测试`scripts/build_corner_label_comparison_review.py`，能按平均角点偏差排序，生成整图/局部双版本叠加、双方原始标签副本、离线选择页和结果索引；1项回归测试与py_compile通过。
- 已生成前30份高差异人工确认包并视觉抽查第1/15/30份，中文和局部放大均清晰。最终94个文件、30预览、60标签副本、约8.18MB，复制到Downloads后逐文件diff一致；原数据未修改。
- 用户确认双版本高差异包30份全部为服务器版更准确；已确认743份中未与服务器重叠的剩余池为253份（205/27/21）。
- 已实现`scripts/build_corner_single_version_review.py`及1项测试，从253份稳定等距抽样30份，生成带V1-V17编号的整图和三段局部放大、标签副本与断点保存HTML；测试和py_compile通过。
- 最终包30预览、30标签、64文件、约10.51MB，抽查第1/15/30份清晰；复制到Downloads后diff一致，训练数据未修改。
- 用户反馈743版未重叠抽样的标注基本不准确；据此启动服务器891份净新增候选的独立抽样复核，但未据此自动导入或替换数据。
- 单版本核对工具已扩展`server891`模式：排除匹配TSV服务器列中的490份，包含V0/C7显示，并为页面使用独立的本地进度键；新增服务器模式回归测试，当前2项测试均通过。
- 抽样算法改为按split规模比例分配后在各split内稳定等距抽取；真实复算候选池891份，抽取30份为train 24、val 3、test 3，30份均含完整V0-V17。
- 已生成并检查30张可解码预览及30份标签副本；视觉抽查第1/15/30份清晰。最终64个文件约9.68MB，已复制到`/Users/liruirui/Downloads/服务器891份净新增角点抽样人工确认包_30份`并逐文件核对一致；原数据未修改。

## 2026-08-12：服务器891份净新增角点融合

- 用户人工抽查后确认891份服务器净新增样本基本正确，授权将其正式融合进`pose_corner_data`训练数据集。
- 用户随即澄清：旧743份整体不准确，不能继续保留；上一条“纯新增891”的理解作废，且此时尚未修改任何训练数据。
- 正确目标是把旧743份全部移入可恢复隔离目录，以服务器去重后的完整1381份替代，再保留新的人工标注871份；理论最终总量2252，分区为train 1976、val 137、test 139。
- 服务器标签限定为YAML声明的class 0-17，过滤class 18/19行；对服务器唯一缺class 12的样本保留并在清单与报告中明确告警。
- 已实现`scripts/rebuild_corner_dataset_with_server.py`和3项测试，具备规模安全门槛、配对检查、超类过滤、角点几何检查、预演、失败回滚、旧数据隔离哈希、正式manifest及重建后独立复验；测试和py_compile通过。
- 真实预演精确识别旧743、人工871和服务器1381，目标文件名冲突0；服务器与人工集SHA-256精确重叠0，服务器内部去重后精确重复0。
- 已完成正式替换式重建：旧743移入可恢复隔离目录，服务器1381按原split复制并清洗标签，人工871保持不变。当前`pose_corner_data`为1976/137/139，共2252份。
- 全量验收完成：2252张图像全部解码，标签问题0，精确重复0，跨split精确重复0；唯一缺V12样本已记录。旧743的1486个隔离文件全部通过manifest SHA-256校验。

## 2026-08-12：284组重复标注人工结果复核

- 用户提供Downloads中的`重复标注核对结果_1786519679959.csv`，说明这是上午284组重复影像人工核对页面导出的结果，希望判断是否还能据此增加训练数据。
- 本阶段先只读解析人工选择并与当前六点、角点数据按任务分别去重，精确测算净新增量；未获进一步明确授权前不直接导入。
- 人工CSV与核对包manifest的284个组号和SHA-256全部一致：114组选图1、149组选图2、21组neither；5组neither可从备注解析出六点/角点分别选择不同版本。
- 已实现并测试`scripts/analyze_duplicate_review_results.py`，复用既有SHA缓存并按assignment_all、任务完整性、几何、左右规范和当前数据内容去重；4项解析测试及py_compile通过。
- 最终测算六点可净新增263、角点可净新增247；拟新增目标文件名冲突均为0。六点另需移出2份人工否决的既有代表，角点需替换1份同图旧标签。
- 已生成`docs/duplicate_review_training_increment_analysis.md`，给出排除项和预计规模；本轮未修改任何训练图像或标签。

## 2026-08-12：应用284组人工核对结果

- 用户确认分析报告并授权正式导入：六点新增263、角点新增247；隔离六点G0090/G0202既有代表；备份并替换角点G0247现有标签。
- 将先生成可恢复计划和备份，再正式应用；新增样本只进入train，现有val/test保持不变。
- 已实现`scripts/apply_duplicate_review_import.py`和测试，支持规模门槛、预演、逐文件哈希、失败回滚、六点隔离、角点标签替换备份及导入后全量审计；1项测试、py_compile和预演均通过。
- 正式应用完成：263份六点和247份角点写入train；G0090/G0202两对六点文件移入隔离目录；G0247角点旧标签备份后替换。
- 全量严格验收通过：六点1755（1456/149/150）、角点2499（2223/137/139）；图像解码和标签问题0，精确重复及跨split精确重复均为0。

## 2026-08-12：患者级数据泄露补充审计

- 用户询问本次训练数据是否检查数据泄露。此前已检查SHA-256精确同图跨split为0，但尚未覆盖同患者不同日期/不同图像。
- 按assignment_all患者ID复核六点后发现`SCO2003P1027`跨train/test：train为`eap_285_..._20160714`，test为`eap_1924_..._20160704`；这是患者级泄露，精确图像哈希检查不会发现。
- 按服务器数字患者号复核角点后发现数字ID在train/val/test之间有明显交叉；正在汇总唯一泄露患者数、涉及样本数和是否同日检查。
- 汇总完成：角点有39名唯一数字患者跨split，涉及train 61、val 25、test 24共110张；6名同时位于三个split，跨split同日检查为0。泄露来自服务器原1381分区，不是本次新增247份人工角点造成。
- 当前应修正此前结论：两套数据精确同图泄露为0，但患者级并非0。已写入`docs/dataset_leakage_audit_20260812.md`；未自动移动数据。

## 2026-08-12：按患者整组重分

- 用户授权修正患者级泄露，并要求维持约8:1:1比例。目标为六点1404/176/175、角点1999/250/250。
- 策略为优先保留现有test患者整体归test，其次保留val患者整体归val，再从train-only患者组补足精确目标；无法解析患者身份的样本按独立组保留原split，避免无依据关联。
- 角点人工患者若在六点已有val/test归属，将优先沿用该归属，使两模型评估患者尽量一致。
- 已实现`scripts/resplit_datasets_by_patient.py`和2项测试。修正首轮把DICOM UID误作患者键后，正式规则只使用assignment、业务患者号和服务器数字患者号；纯UID样本固定原split。
- 最终预演精确命中六点1404/176/175和角点1999/250/250，预计移动52对与231对；患者组不拆分。
- 正式移动完成并全量复验：可识别患者泄露0、精确重复0、跨split精确重复0、图像和标签问题0；283对迁移文件SHA-256全部一致。

## 2026-08-12：datasets当前数据集总说明

- 用户要求在`datasets/`下单独编写文档，描述当前训练数据如何构建、包含内容、分布和质量。
- 文档将以当前活动文件重新统计为准，覆盖`pose_data`与`pose_corner_data`，并明确历史清洗、人工核对、服务器替换、患者级重分和审计边界。
- 已新增`datasets/DATASET_README.md`，统一记录当前1755份六点和2499份角点的目录、标签格式、来源构成、8:1:1分布、图像尺寸/通道、visibility、类别完整度、构建时间线、质量验收与已知风险。
- 文档已逐项对照活动图像/标签和`6-train_ap_model`下实际训练YAML复核；补充指出角点唯一缺V12样本、服务器/人工bbox约定差异、各412份未知患者标识的审计边界，以及数据目录内历史错误角点YAML不可用于训练。

## 2026-08-12：统一角点bbox定义

- 用户确认将`pose_corner_data`全部bbox统一为同一规则：每行bbox取TL/TR/BR/BL四点的水平最小外接框，不添加额外边距。
- 安全约束：只允许修改每行第2–5字段`cx cy w h`；class、四点坐标、visibility、行序、图像和split均保持不变。正式修改前备份全部2499份标签并记录SHA-256。
- 已实现`scripts/normalize_corner_bboxes.py`及3项测试；py_compile与测试通过。真实预演确认2499份、44981行中恰有1381份服务器标签的21253行需修改，人工1118份保持原字节不动。
- 正式执行完成：修改前标签树SHA-256为`1a39d541...e6ebb8f`，统一后为`75b6e465...39f645`；图像树SHA-256前后均为`7559f65d...dde73d`。
- 独立逐文件复验2499份备份及活动标签哈希全部匹配manifest；非bbox字段变化0、未规范bbox行0，split仍为1999/250/250，类别计数保持V12为2498、其余各2499。重复预演显示changed_files=0、changed_rows=0，3项测试继续通过。
- 已更新`datasets/DATASET_README.md`与训练权威配置注释，并新增`docs/corner_bbox_normalization_report_20260812.md`记录规则、修改范围、整体哈希、备份位置、验证结果和复现命令。

## 2026-08-22：六点Pose数据集几何与黑边审计

- 完成1755张原图及标签的只读全量扫描，输出逐图CSV/JSON与分组汇总到`/private/tmp/pose_geometry_audit_20260822.*`和`/private/tmp/pose_geometry_summary_20260822.json`。
- 完成分辨率、通道、宽高比、`eap_`/旧来源、train/val/test、阈值5/12/20连续黑边及`imgsz=800`方形letterbox换算。
- 视觉检查最大黑边train样本和test代表样本，确认统计识别的是原始X光画幅外的真实黑背景；活动数据、标签和训练配置均未修改。
- 完成771张多通道图的通道一致性与alpha检查、六点标签纵向几何统计，并把175张test核验误差与逐图黑边结果关联。
- 确认黑边在`eap_`内部与归一化误差近似无相关；确认简单均值阈值会在2张train图上切到真实关键点，不能直接用于自动裁边。
- 新增专项报告`docs/pose_dataset_geometry_audit_20260822.md`，阶段127–130完成；本轮始终保持训练图像、标签和配置只读。

## 2026-08-22：六点Pose黑边可疑样本可视化包

- 启动阶段131–134，目标是在E盘生成可筛选的离线黑边人工核对包，不修改活动训练图像或标签。
- 已核实唯一横图是被标尺/文字打断的横向黑画布，需作为连续暗边规则的补充候选纳入。
- 阶段131–132完成：连续暗边≥5%为主规则，横向黑画布为窄补充规则；生成器与3项回归测试已通过，预览明确标出红色统计暗区、绿色拟保留框、青色六点标注和关键点裁剪风险。
- 首次真实生成得到488份候选，数量结构为连续暗边487、横向黑画布补充1；视觉抽查发现JPG内中文字体显示方框，已改用英文烧字并准备重生成，避免交付不可读预览。
- 已完成第二轮真实生成与代表图复查：488张预览全部生成；最大暗边、关键点裁剪风险、横向黑画布三类显示正确。阶段133完成，进入全量解码、清单资源和E盘副本一致性验收。
- 阶段134完成：临时包488张预览全量解码和哈希通过；归档到E盘后清除仅属于新包的493个AppleDouble旁车，最终493个内容文件与临时包逐文件一致，训练数据未修改。

## 2026-08-22：六点Pose原图+ROI裁剪视图混合训练集

- 用户授权正式执行；已启动阶段135–139。先固化变换与安全门槛，再实现生成器、训练入口和真实派生数据集，原始1755份六点数据不覆盖。
- 阶段135完成：确定利用Ultralytics多train目录原生能力引用原图，派生目录只生成全train一对一ROI视图；val/test原样，采用确定性扰动、标签几何换算与失败不落正式目录的构建边界。
- 阶段136完成：生成器与4项测试通过；真实预演为1404张ROI、混合train 2808，未写入派生数据。进入训练入口与低增强预设改造。
- 阶段137完成：Python和Shell训练入口均可选混合YAML与`roi_low`预设，默认旧训练行为不变；新增YAML和4项配置测试均通过。开始正式生成1404张ROI视图。
- 阶段138完成：正式生成1404张ROI图/标签约4.7GB，几何反算、全量解码和逐文件哈希错误均为0；三类代表图视觉抽查通过。进入Ultralytics实际加载与最终交付验收。
- 阶段139完成：Ultralytics无缓存实载混合YAML得到train/val=2808/176、关键点(6,3)、标签问题0；文档和复现命令已补充，阶段135–139全部完成。

## 2026-08-22：本地两阶段Pose推理

- 用户要求先完成本地inference和测试，再决定线上改造；已启动阶段140–143，`xiehe-system`保持不变。
- 阶段140完成：确定不改已有脏状态的Pose/Detection对比脚本，新增独立两阶段核心与本地CLI；最新旧权重只做链路冒烟，正式175张效果比较留到ROI混合模型训练完成后。
- 阶段141–142完成：两阶段核心、本地批量CLI和7项回归测试通过；未修改`xiehe-system`。
- 阶段143完成：3张代表图均成功二阶段并通过可视化坐标回写检查；旧的原图训练权重二阶段误差由18.45 px增至28.46 px，因此仅确认链路正确，不进入线上。已补齐无检测安全聚合和新模型175张正式评估说明。

## 2026-08-22：Corner ROI增量混合训练集

- 用户授权执行，新增阶段144–148。先把现有Pose ROI与Corner train按源图哈希和角点包含关系做正式分类，再实现增量生成、训练入口、真实落盘与全量验收；不重建原始数据，不训练模型，不改线上。
- 阶段144几何规则已确定：安全匹配复用现有像素；不安全样本扩展原Pose框；缺失样本按1252条真实ROI/Corner范围比例生成带扰动的Pose风格大边距框。下一步实现源图哈希门槛和增量manifest。
- 阶段144–145完成：增量生成器和5项测试通过，已覆盖源图/ROI哈希门槛、硬链接、重裁、标签换算、首次临时构建及重复执行跳过。进入Corner混合训练入口配置。
- 阶段146完成：Corner Python/Shell入口和混合YAML已就绪，默认旧训练行为不变；3项配置测试和入口检查通过。进入真实全量预演与增量落盘。
- 阶段147预演通过：正式哈希分类为1212复用、40扩框、747缺失生成，新写像素787；输出路径为空且磁盘容量充足，准备首次临时目录构建。
