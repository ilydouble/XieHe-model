# 训练导出数据审计计划

## 目标

判断 `training_export` 中是否存在重复图像或重复样本，并在当前仓库实现一个可重复运行的标注质量检查脚本、测试和使用说明。

## 范围与约束

- 原始导出目录只读，不修改、不重命名任何文件。
- 避开工作区中已有的无关未提交改动。
- 以实际标注格式和目录结构为依据制定质量规则。
- 当前任务产生的文件单独提交。

## 阶段

- [complete] 1. 盘点导出目录、标注格式和现有仓库约定
- [complete] 2. 用内容哈希与标注对应关系分析重复数据
- [complete] 3. 设计并实现标注质量检查脚本
- [complete] 4. 添加测试、运行真实数据审计并整理报告
- [complete] 5. 提交本任务变更并交付结果

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|---|---:|---|
| 首轮重复明细过长，汇总被输出截断 | 1 | 改为单独计算汇总并只显示少量代表组 |
| 首轮脚本把 1551 个 `.png` 判为无效 PNG | 1 | 检查实际文件魔数；审计器改为按内容支持 PNG/JPEG，而非只信扩展名 |
