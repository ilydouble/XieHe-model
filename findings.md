# 训练导出数据审计发现

## 数据结构

- 导出目录为单层目录，共 3246 个文件，总大小约 2.8 GiB。
- 当前观察到的文件主要为 PNG 图像和以 `_label.json` 结尾的标注。
- 文件名通常为 `<导出ID>_<检查号或原始名称>.png`；同一检查号可能带不同导出 ID，这解释了“名称重复”的一种可能来源，但仍需以内容哈希确认。
- 少量名称包含中文、空格或 `AP`，脚本必须支持 Unicode 路径且不能只依赖固定命名模式。
- 精确类型计数：1670 张 PNG、1576 份 JSON 标注，因此至少有 94 张图像没有按同一导出 ID 配套的标注（最终配对数仍需脚本确认）。
- 抽样 JSON 顶层字段稳定为 `imageId`、`originalFilename`、`imageWidth`、`imageHeight`、`vertebrae`。
- `vertebrae` 是混合标注列表：点标注使用 `type: point` 与 `point: {x,y}`；椎体相关条目使用 `type: vertebra` 与 `corners`。坐标看起来为 `[0,1]` 归一化值。
- 椎体条目名称可形如 `C7-1`，抽样中其四个 `corners` 坐标完全相同，说明当前导出结构可能用重复坐标承载单个角点；质量规则不能武断地把“同一条目四角重合”直接判为坏标注，而应作为结构特征或可配置告警。
- `originalFilename` 的扩展名可能是 JPG，而实际导出图统一为 PNG；配对必须优先使用导出 ID/标注文件 stem，不能直接拼接原始文件名。

## 仓库约束

- 本仓库要求每次代码或文档变更完成后立即提交，且不得混入无关工作区改动。
- 当前已有无关改动：`.gitignore`、`8-test_model/compare_pose_vs_detect.py`、`8-test_model/_check_black_border.py`。

## 重复数据

- 已用 SHA-256 对 1670 张 PNG 做精确内容哈希，确认存在大量完全相同的图像内容组；这不是单纯文件名相似。
- 标注中的 `originalFilename` 也大量重复，组大小以 2 为主，至少有 4 组大小为 3（例如多个检查号/原文件名被重复导出）。
- 仍需得到未截断的精确汇总，并比较重复图像所关联的标注内容：图像重复但标注不同的样本不能直接按文件删除，需先决定保留/合并策略。
- 1576 份 JSON 均可被 `jq` 正常解析，未发现语法损坏。
- 精确汇总：304 个重复图像组，涉及 611/1670 张图；每组只保留 1 张时有 307 张冗余副本，最大重复组大小为 3。
- 重复不仅发生在相同检查号上；至少发现一组不同检查号但字节完全相同的图像，说明需要在报告中保留每组路径供人工核验，不能只靠检查号去重。

## 标注质量规则

- 已确认应覆盖：JSON 可解析性、必填字段/类型、图像配对、图像尺寸一致、坐标有限且在 `[0,1]`、标签唯一性、点/椎体结构、来源字段、标注数量与标签集合异常。
- 是否把缺失标签、解剖顺序和左右关系设为 error 或 warning，需要通过全量分布确认。
- 1576 份标注全部存在同 stem PNG；因此有 94 张 PNG 没有 JSON 标注。
- 标注项数量分布高度集中：1441 份恰好 78 项，1 份 77 项，其余 134 份介于 2–76 项。78 项的主结构与 72 个椎体角点标签（C7、T1–T12、L1–L5，各 1–4）加 6 个点标签（CL/CR、IL/IR、SL/SR）吻合。
- 全量类型仅见 `point`（9179 项）与 `vertebra`（109170 项），来源仅见 `ai`（44611 项）与 `manual`（73738 项）。
- 实施策略：结构/解析/尺寸/坐标非法作为 error；缺少期望标签、左右或解剖几何异常、重复图像标注冲突作为 warning；不自动修改或删除数据。

## 首轮脚本实跑

- 重复图像的标注状态：291 组 `conflicting`、12 组 `partial`、1 组 `unlabeled`，0 组完全一致。说明重复导出记录之间的人工/AI 标注内容普遍发生变化，不能安全地只按哈希批量删副本。
- 首轮报告出现 1551 个 `invalid_png`，根因很可能是大量文件虽以 `.png` 命名，实际字节仍为 JPEG（`originalFilename` 普遍是 `.jpg`）。这是审计器格式识别假设过窄，不应直接算数据损坏；下一步检查魔数并支持 JPEG 尺寸读取。
- 排除上述格式识别问题后，当前真实质量问题至少包括：7 个越界坐标、21 个椎体四角顺序告警、8 个左右点顺序告警、135 份缺少标准标签、94 张无标注图像。

## 最终审计结果

- 按魔数识别到 1551 张 JPEG 与 119 张 PNG；所有 1670 张图均可读取尺寸。扩展名 `.png` 并不代表真实编码格式。
- 最终 error 共 10 个：7 个归一化坐标越界，分布在 5 份标注中；3 个图像尺寸不一致，且均表现为 JSON 宽高与实际宽高互换，疑似图像旋转后元数据未同步。
- warning 共 549 个：291 个重复图像标注冲突、135 份缺少标准标签、94 张无标注、21 个椎体四角顺序可疑、8 个左右点顺序可疑。
- 291 个重复图像标注冲突全部属于 `coordinates_or_structure`，不是仅 `source` 字段不同；12 组重复图像只有部分副本有标注，1 组全部无标注。
- 标注中的重复 `originalFilename` 共有 731 组；这一指标反映重复导出/同源记录，但去重仍应以图像哈希和标注比较为准。
- 完整本地 JSON 报告生成于 `/private/tmp/training_export_audit.json`，其中可能包含敏感原始文件名，因此不提交 Git。

## 旧训练集盘点（进行中）

- 仓库中存在 `datasets/pose_corner_data`、`pose_data`、`pose_det_data`、`seg_data`；训练脚本明确引用这些目录。
- `pose_corner_data` 是当前唯一有实质训练规模的数据集：train 594、val 74、test 75，共 743 对图像/标签。
- `pose_data` 当前 train/val 各 1、test 231；`pose_det_data` train/val 各 1、test 156；`seg_data` 每个 split 各 1。若目标是继续训练椎体四角模型，主要比较对象应是 `pose_corner_data`。
- 旧数据使用 YOLO TXT，新增导出使用包含 78 项的 JSON，两者不能直接按标注文件内容比较，需要转换到统一的语义坐标表示后再做质量和一致性检查。
- `6-train_ap_model/corner_data.yaml` 注释表明 `pose_corner_data` 已包含一次 `202605AP_PUMCH_Data` 新增数据，因此必须检查本次 `training_export` 是否与旧训练集图像重叠，避免把重复样本当作增量。
- 旧 `pose_corner_data` 标签为 YOLO Pose：每行一个椎体，字段为 class、bbox、TL/TR/BR/BL 四角点及可见性；类别为 V0–V17。
- 抽样旧标签常从 class 1 开始，提示 C7/V0 可能在部分来源中缺失或刻意留空；不能简单要求旧数据每张都恰好 18 行。
- 旧椎体训练图与新导出图的 SHA-256 精确交集为 0；旧六点训练图则有 1 张精确交集。旧图仍可能经过 DICOM 转 PNG、缩放或重编码，因此继续做感知相似度检查。
- `pose_corner_data` 三个 split 有 743 对图像/标签；目录内另有 1 张 `visualize/` 可视化图，所以递归图像扫描会读到 744 张。训练 split 内部无精确重复。
- 旧 YOLO 标签结构检查结果：742 份为 17 行，1 份为 16 行；字段数、类别范围、归一化坐标、可见性和单文件类别唯一性均合法。
- 所有 743 份旧标签都包含 class 1–11、13–17；class 12 仅一份缺失；class 0 在全部旧数据中都不存在。也就是说旧四角训练集实际训练的是 17 个椎体类别，而配置宣称 18 类，C7/V0 没有训练样本。
- 当前系统 Python 没有 Pillow、NumPy、OpenCV 或 imagehash；近似图像比较需采用系统已有的图像命令、仓库其他运行环境，或实现无依赖的解码路径。
- 系统存在 FFmpeg 与 sips；Codex 工作区自带 Python 环境包含 Pillow 和 NumPy，可用于只读计算感知哈希，不需要给项目安装新依赖。

## 两个模型链路核实

- 6 点任务实际同时存在两条实验链路：`train_pose.py` 训练 1 类 + 6 keypoints 的 YOLO Pose；`train_detection.py` 训练 CR/CL/IR/IL/SR/SL 六类 YOLO Detect，并取检测框中心作为点。
- `8-test_model/compare_pose_vs_detect.py` 正在对比六点 Pose (`runs/pose/best_performance2`) 与六点 Detect (`runs/detect/best_performance-3`)；因此用户记忆的“六点似乎使用 Detection”对应当前对比/候选方案，但仓库仍保留 Pose 方案。
- 椎体角点任务明确是 YOLO Pose：18 个椎体类别，每类 4 个关键点，训练脚本为 `train_corner.py`，候选权重为 `runs/corner/best_performance7/weights/best.pt`。
- `3-inference/app.py` 当前生产示例仍加载 `weights/pose.pt` 作为六点 Pose，而不是六点 Detect；必须继续核实实际导出系统 `XieHe-System/backend` 使用的模型，不能把本仓库实验脚本等同于导出标注来源。
- 椎体推理代码并非直接信任模型类别，而是按 y 坐标重新排序后命名为 V0、V1…；若漏检一节或出现假阳性，后续所有椎体编号可能整体错位，这与用户观察到“脊柱点受干扰、错位”高度吻合。
- 导出脚本只导出数据库中的当前 `vertebraeLayer`，不会同时保存最初 AI 预测，因此现有 JSON 不能直接恢复“人工修改前后位移”。
- 前端 `handleVertebraeUpdate` 使用 `markMovedKeypointsManual`：只有移动过的关键点会从 AI 标为 MANUAL，未移动点保持 AI；测试也验证同一椎体可出现 1 个 manual + 3 个 ai。因此 `source` 可用于识别人工移动过哪些点。
- 但 `source=ai` 只表示“未被移动”，不等于人工确认正确；`source=manual` 表示被移动/手动添加，也不保证最终位置正确。
- 要回答“标注与 AI 是否一致”，应使用对应模型重新推理，与当前导出坐标逐点比较；对 `source=ai` 的一致很可能是同源模型复现，只能验证推理/保存链路稳定，不能单独证明真实准确率。
- 六点 Detect 数据不是独立人工标注：`convert_pose_to_detection.py` 直接把六点 Pose 标签转换成 6 个固定大小框。因此 Pose 与 Detect 的训练真值来源相同，两个模型互相一致也不是独立证据。
- 当前磁盘上的六点数据 split 已变成 Pose: train=1/val=1/test=231，Detect: train=1/val=1/test=156，与历史训练时显然不一致；仅凭现存目录无法复现历史训练指标，需查看提交历史或原始数据构建记录。
- 历史结果中六点 Pose 的验证 pose mAP50-95 约 0.975、椎体角点 Pose 约 0.977，而六点 Detect box mAP50-95 约 0.334。由于验证标签本身未经人工确认且可能存在数据重叠，这些高 mAP 不能直接解释为临床点位准确率。
- 本机默认 Python 缺少 ultralytics、Torch、OpenCV；权重存在但需要找到原训练 Conda 环境或建立明确环境后才能安全重跑 AI。
- 已找到 `/opt/miniconda3/envs/cv`，包含 ultralytics、Torch、OpenCV，可用于加载候选权重；全量推理前仍需做少量样本验证。
- 新导出与旧六点 `pose_data` 有 1 张 SHA-256 完全相同图像；与旧椎体 `pose_corner_data` 无字节级交集，但 512 位感知指纹发现 1 张完全相同内容。两者实际是同一张新图 `137_4322078009__CR_TSPINE_20210202_slice0000.png`，它同时存在于两个旧模型数据集中。
- 该重叠样本的旧六点标签与新导出六点严重不一致：6 点归一化欧氏距离约 0.176–0.908；旧标签全部挤在图像左上角，而新导出为全 AI 点。这直接证明旧六点数据至少存在一个严重错配/归一化异常，不能默认旧训练集标注正确。
- 同一重叠样本的旧椎体 68 个共同角点与新 AI 角点平均归一化距离约 0.038、最大约 0.065，全部超过 0.01，也不是舍入误差；两套标注不能同时当真值。
- 新导出按任务拆分：六点完整 1496/1576、不完整 80；椎体 72 点完整 1450/1576、不完整 126。
- 六点来源分布：1303 份全 manual、30 份全 AI、163 份混合、80 份不完整；椎体来源分布：108 份全 manual、164 份全 AI、1178 份混合、126 份不完整。
- 304 个精确重复图像组按任务比较后仍无坐标完全一致组：六点 281 组冲突、22 组部分标注、1 组无标注；椎体 263 组冲突、40 组部分标注、1 组无标注。
- 旧六点现存 233 份标签格式完整，但发现 75 个越界坐标、83 份六点全部位于图像左上 20% 区域；这表明旧六点标签存在系统性质量问题。左右 x 关系异常很多，但需结合影像显示约定人工判断，暂不作确定错误。
- 已用 `cv` 环境成功加载权重并确认：六点 Pose 为 1 类 + `[6,3]`；六点 Detect 为 CR/CL/IR/IL/SR/SL 六类；椎体模型为 V0–V17 十八类 + `[4,3]` Pose。

## 四项审计结论

- 新批次内部除了 304 组 SHA-256 完全重复外，512 位缩放鲁棒指纹还找到 297 对距离不超过 20 的非字节相同候选；其中大量同尺寸图解码后像素完全相同或仅有轻微 JPEG 重编码差异。说明同图可能因重新编码而逃过纯 SHA 去重。
- 对近似重复不能自动删除：精确重复组中六点有 281 组坐标冲突、椎体角点有 263 组坐标冲突。正确流程是先按图像内容聚成病例/影像组，再人工或规则选定一份规范标注，最后每组只进入训练/验证/测试中的一个 split。
- 标注配对层面有 94 张图没有 JSON。1576 份 JSON 中，六点完整 1496 份、不完整 80 份；72 个椎体角点完整 1450 份、不完整 126 份。另有 7 个坐标越界和 3 个宽高互换硬错误，因此不能整批直接训练。
- 对 120 张抽样图复跑现存候选权重：椎体模型按预测类别匹配导出角点的归一化距离中位数 0.00261、P90 0.01506；`source=ai` 中位数 0.00036，`source=manual` 中位数 0.00478。AI 源点高度一致主要说明候选权重与导出模型同源或近似，以及保存/重跑稳定，并不是独立准确率证明。
- 同批椎体模型按纵向位置重新编号时，中位距离升至 0.00315、P90 0.05381；120 张中有 5 张未检出完整 18 节（1 张 0 节、1 张 12 节、1 张 14 节、2 张 17 节）。漏检或假阳性会使后续椎体整体编号错位，是用户观察到脊柱点受干扰的重要机制。
- 现存六点 Pose/Detect 候选与导出六点严重不一致：Pose 中位距离 0.73586、Detect 中位距离 0.71384，且分别缺少 258/320 个可比较点。类别顺序和归一化坐标映射已复核无误；结合旧六点训练标签的越界及左上聚集问题，这两个旧权重不能作为本批六点标注的质量裁判。
- 实际 `XieHe-System/model/ap` 服务代码配置为六点 Pose + 椎体四角 Pose，并未使用六类 Detect；六点 Pose 失败或点聚集时会由椎体角点估算六点。线上 `weights/pose.pt` 与 `weights/pose_corner.pt` 不在当前工作副本，无法证明历史导出使用的是哪一版权重。
- 用当前导出椎体角点复算服务中的六点回退公式，与 `source=ai` 六点中位距离 0.356，明显不吻合；因此导出六点并非主要由当前版本回退公式直接生成，更可能来自另一版六点 Pose/服务逻辑。
- 缩放/转码鲁棒比较确认旧数据重叠仍只有 1 张新图：它与旧六点图指纹距离 0，与旧椎体图距离 6；其他新图到两个旧集最近距离均大于 48。旧椎体递归扫描虽包含 1 张 `visualize/` 图，但命中的已知重叠来自 `images/train/`，不影响结论。现有阈值下没有发现第二张旧/新同图。
- 训练选择原则：专家确认的完整标注可进入正式训练；经确认的人工修正且与模型分歧较大的样本通常最有增量价值；AI-only 且与同源模型一致的样本只能作为低权重伪标签候选，不能进入验证/测试集；缺标、越界、重复冲突、椎体数量异常或错位样本先隔离复核。

## 异常可视化与左右翻转核验（进行中）

- 21 个椎体角点顺序告警实际对应 15 张图、21 个异常椎体，不是 21 张不同图。
- 8 个左右关系告警实际对应 4 张图：两张图三组点全部反向，另外两张仅 SL/SR 反向。
- 全量主流关系为 CL<CR 1513/1515、IL<IR 1520/1522、SL<SR 1548/1552；告警是少数语义或变换链路例外，需要结合图像方向和历史翻转代码判断。
- 本阶段不处理 7 个越界坐标，只生成椎体与左右告警预览并核实原因。
- 前端上传与批量导入支持在上传前把图像像素做水平翻转；已有影像的编辑入口也会生成水平翻转后的新文件并替换对象内容。当前编辑保存流程会要求确认并清除本地标注缓存，但导出的训练 JSON 没有记录 `flipped` 元数据，无法仅凭 JSON 追溯历史上是否翻转。
- AP/侧位批量 AI 脚本另有 `--lr-flip`，只在推理前翻转输入；其 raw JSON 坐标明确对应翻转后的图像。若后续保存的图像、坐标和 L/R 标签没有使用同一坐标系，会产生当前这种左右关系反向。
- 因此水平翻转可以解释 8 条左右告警，但不能据此自动判为正确：必须查看影像中的 L/R 铅字标记、当前保存图像和点位，并确认业务标签是解剖学左右还是画面左右。
- 已在 `/tmp/标注告警人工复核` 成功生成 21 张椎体异常明细图（对应 15 张原图）、4 张左右翻转对照图、CSV 索引和 HTML 浏览页，待视觉抽查后归档到 E 盘。
- 视觉抽查确认叠加图可读。`248.../L3` 的异常版本实际采用近似 1=左上、2=左下、3=右上、4=右下的顺序，同图 `218...` 则符合 1=左上、2=右上、3=左下、4=右下，属于真实角点编号版本冲突。
- `579.../T1` 的 1/2 与 3/4 两组相距多个胸椎高度，形成异常长矩形；同图 `1575...` 的 T1 四点集中在同一椎体，属于明确错位而非正常倾斜。
- `165...` 当前图的 `L` 铅字在画面左侧，但 AI 的 CL/IL/SL 全在画面右侧；镜像模拟后两者共同换边。若 L/R 标签代表解剖学左右，则当前 AI 标签与铅字不一致，不能因“可能翻转”直接判正常。
- `2115...` 只有 SL/SR 反向，而 CL/CR、IL/IR 均符合主流关系；整幅图水平翻转会同时改变三组关系，因此整图翻转不能解释该样本的局部异常。其同图 `2081...` 的 SL/SR 正常，强烈提示局部标反。
- 最终复核包已归档至 `/Volumes/E/spine_data/20260810-正面-已处理/标注告警人工复核`，包含 21 张椎体明细 JPG、4 张六点左右对照 JPG、HTML 浏览页、CSV 索引和 Markdown 说明；已清除本次复制生成的 AppleDouble 文件。

## 人工确认后的数据修正（进行中）

- 用户已人工确认 4 张六点左右告警图应采用水平翻转后的图像方向；修正必须同时翻转像素和所有标注坐标 `x→1-x`，保持点与解剖结构对齐。
- 用户已确认椎体异常的重复图另一版本正确：规范化训练目录应删除错误重复图及对应 JSON，仅保留正确版本。
- 特殊重复组 `1575/579` 中，`1575` 的 L4 异常而 `579` 的 T1 异常；不能直接任选一份。计划保留较新导出 ID `1575`，用 `579` 的正确 L4 替换后删除 `579`，从而保留两者经人工确认的正确部分。
- 没有重复版本可替代的异常实际为 5 张图、7 个椎体：`1163/T1`、`1626/L4`、`1874/T4,T9`、`554/T10,L2`、`999/L1`。将只重排这 4 个既有坐标对应的标签编号，不移动坐标数值。
- 删除和覆盖前必须在数据集外建立带清单的恢复备份；7 个越界点继续保持不动。
- 修正干运行已解析出 32 个受影响文件。无重复异常的自动重排映射为：上下两对互换 4 个椎体；T10 交换 1/2；L2 交换 3/4；L1 使用旧点顺序 `[1,3,2,4]` 生成新 1–4。
- 已在 `/tmp` 的 32 文件副本上完整试运行并验证：4 张图像像素逐列镜像完全一致，全部 JSON 坐标满足 `x_new=1-x_old` 且 y 不变；7 个椎体重排后通过左右/上下几何规则；特殊组 L4 与 donor 完全一致；9 组目标图和 JSON 均被移除。正式 E 盘操作可以按同一脚本执行。
- 正式镜像后的首次复审发现 4 张镜像图新增 54 个椎体角点顺序告警，原因是像素和坐标镜像后还需同步画面角点语义。已对每节完整椎体执行 `1↔2、3↔4`，并把补充 SHA 清单和最终脚本写入恢复备份；再次快速复审后椎体顺序告警为 0。
- 最终数据集统计：1661 张图、1567 份配对 JSON、94 张无 JSON；7 个越界坐标保持未处理；左右关系剩余 2 条，均来自用户人工确认方向后的 `2115...json`，是固定画面左右规则无法表达的已知例外。
- 最终 SHA 审计：295 个精确重复组、涉及 593 张图、冗余 298 张；其中 282 组仍有坐标或结构冲突、12 组部分有 JSON、1 组全无 JSON。本轮只删除了人工确认错误的 9 个重复样本，没有擅自解决其余冲突。
- 主报告已更新，完整修正后审计归档为 `/Volumes/E/spine_data/20260810-正面-已处理/training_export_normalized_post_review_audit.json`。恢复备份包含 32 个原文件、原始清单、补充角点镜像清单和最终修正脚本。

## 无标注图像移出（已完成）

- 用户确认将最终审计中的 94 张无 JSON 图像移出监督训练工作目录，不永久删除。
- 目标目录为归档根目录下的 `待标注_94张/`；将附 SHA-256 迁移清单，便于补标后核对和恢复。
- 移动后 `training_export_normalized/` 应剩 1567 张图和 1567 份配对 JSON，无标注图像应为 0；原始 `training_export/` 保持不变。
- 已将 94 张图像移动至 `/Volumes/E/spine_data/20260810-正面-已处理/待标注_94张/`，未删除；目录内含 `move_manifest.json`（94 条文件 SHA-256 与大小）和 `说明.md`。
- 移动后全量复审确认 `training_export_normalized/` 有 1567 张图、1567 份 JSON、1567 对配对，无标注图像为 0；仍有 7 个坐标越界、135 份缺标准标签以及同一人工确认翻转样本中的 2 条左右规则例外。
- 无标注和部分标注的重复副本被隔离后，当前精确重复为 284 组、涉及 569 张图、冗余 285 张；284 组全部为坐标或结构冲突，不存在“仅部分副本有 JSON”或“全组无 JSON”的组。
- 最终审计已归档为 `/Volumes/E/spine_data/20260810-正面-已处理/training_export_normalized_labeled_only_audit.json`，主分析报告已同步更新；写入产生的本次 AppleDouble 旁车已清除。

## 两个模型独立训练清单（进行中）

- 用户确认六点模型与脊柱 Pose 模型允许使用不同图像，因此将对同一份 JSON 中的两个任务分别判定，不再要求样本同时满足 6 点和 72 点标准。
- 六点高质量候选初始规则：6 个标准点完整、坐标合法、左右关系通过或属于已人工确认方向例外、6 点均为 manual；纯 AI、mixed、缺点或大幅重复冲突不直接进入高质量候选。
- 脊柱 Pose 高质量候选初始规则：72 个标准角点完整、坐标合法、角点顺序通过；允许 manual 与 mixed，纯 AI 进入待复核而非高质量候选。
- 精确重复组按任务独立处理：任务坐标最大归一化差异不超过 0.005 且所有版本满足该任务质量规则时，以组内 medoid 选一个代表样本，其他副本排除；其余冲突版本全部进入待复核，不随机保留。
- 已实现 `scripts/build_task_training_manifests.py`：对每份 JSON 分别提取 6 点和 72 点，输出包含判定、原因、来源概况、完整点数、重复组和代表样本字段的 UTF-8 BOM CSV，并同时生成机器可读汇总与规则说明。
- 重复组逻辑允许“一个版本任务标注无效、另一个版本有效”时只采用有效版本；多份有效标注只有在全部满足任务来源/几何规则且组内最大点距不超过 0.005 时才自动选 medoid。
- 六点最终清单：812 张可训练、671 张待复核、84 张排除；其中精确重复组内选出 5 张代表样本，均属于“同组仅此版本满足六点质量规则”。当前唯一满足距离≤0.005的双有效六点重复组为 4 manual + 2 AI 的 mixed/mixed，因此按高质量口径仍列为待复核，没有把它误当成全 manual 自动纳入。
- 脊柱 Pose 最终清单：874 张可训练、567 张待复核、126 张排除；精确重复组内选出 17 张代表样本，均属于“同组仅此版本满足脊柱任务质量规则”。
- 两个可训练池交叉关系：743 张两边都可训练、69 张仅六点可训练、131 张仅脊柱可训练，共 943 张至少进入一个候选池，验证了独立判定确实保留了单任务有效样本。
- 清单归档于 `/Volumes/E/spine_data/20260810-正面-已处理/模型独立训练清单_20260811/`；两个 CSV 各含 1567 条且判定互斥完备，所有路径存在，同一精确重复组在每个任务的可训练集合中至多保留一份。归档前后逐文件 diff 一致，AppleDouble 为 0，规范化数据集仍为 1567 图 + 1567 JSON。

## 越界点人工复核（已完成）

- 7 个越界点全部来自六点任务，共 5 份 JSON；脊柱72点没有坐标越界。
- `1257...` 的 IL 为 manual 且 x=1.01453，同时缺 IR；同图 `1880...` 六点完整合法，因此六点模型应使用1880。但1257在脊柱清单中是该重复组唯一满足规则的版本，不能整张删除，应按任务分别取用。
- 其余4份为 `1928...`、`2433...`、`355...`、`532...`：1928/2433各有AI肩点CL、CR越界；355/532是同图两版，各有CR越界。
- 1928/2433的4个AI肩点可由当前 `estimate_pose_from_vertebrae` 的T1中心±4.5倍T1宽度公式精确复现；代码和导出均无边界截断。这是已保存数据的历史问题，同时当前代码仍可能在窄幅影像上复现。
- 已实现 `scripts/render_out_of_range_review.py`：将原图置于扩展灰色画布，以白框标出真实图像边界，越界点用红色双圈显示，其他manual/AI点分别用青色/黄色显示，并输出CSV、JSON和HTML索引。
- 已生成4张真实扩展画布复核图并进行视觉抽查；首次右侧标签会被画布裁切，调整为自动向内避让并把多个越界点在页脚分行显示后，1928和532代表图的全部坐标文字均清晰可见。
- 临时复核包共9个文件：4张PNG、HTML浏览页、CSV索引、JSON清单、Markdown说明和生成脚本；复核范围为4份JSON、6个越界点。
- 复核包已归档至 `/Volumes/E/spine_data/20260810-正面-已处理/越界点人工复核/`；归档与临时目录逐文件一致，4张预览齐全，CSV为表头+6个越界点，AppleDouble为0。规范化数据集仍为1567图+1567 JSON，未删除1257或修改任何标注。
- 用户人工确认：532标注不正确；1928、2433、355的越界点合乎影像裁切和解剖位置逻辑。计划备份并移除错误的532图像及JSON，保留同图355；其余合理越界点不做`clamp`，继续排除出六点训练，但可按脊柱任务规则独立使用。
- 已核实355/532图像SHA-256同为`e54676...7732c`，JSON哈希不同。532图像与JSON已复制到`修正备份_20260811_越界点复核/`并逐字节验证后从规范化训练目录移除；355仍在原位，当前数据集为1566图+1566 JSON。
- 移除532后的全量审计：1566图、1566 JSON、1566对配对，无缺JSON；剩6个越界坐标，分布于1257、1928、2433、355四份JSON；283个精确重复组涉及567图，冗余284图，283组均为坐标或结构冲突。
- 六点清单更新为812可训练/671待复核/83排除；脊柱Pose清单更新为875可训练/565待复核/126排除。355在六点清单中因CR越界而排除，在脊柱Pose清单中为可训练。
- E盘主报告、当前审计JSON、历史审计JSON、两份任务清单及人工确认结果已同步更新；更新前的报告、审计和清单也已放入532恢复备份目录。
- 已验证两份CSV各有1566条唯一记录、所有路径存在、无532、355分流正确；审计与报告的临时版与E盘归档SHA-256一致，本次产生的AppleDouble旁车已清理。

## 旧六点数据集专项复核（进行中）

- `datasets/pose_data` 当前只有 train=1、val=1、test=231 对图像/标签，并非正常的训练分区；这与历史高 mAP 训练记录不相匹配，现有目录更像被重组后的测试/比较集。
- 233份 `pose_data` 标签中，只有158份是合法六点Pose格式（单行、23列、class=0）；另75份是脊柱椎体四角Pose格式（每行17列、通常17个椎体class），被错放在六点数据集的test目录。
- 因此先前把全233份标签当六点解析得到的“75个越界坐标”不是真实六点越界，而是标签任务类型混入造成的误解析。对158份真六点标签重新检查后，六点和bbox坐标越界数都为0。
- 158份真六点标签共948个点，全部可见；visibility=2有498个，visibility=1有450个。未发现点缺失。
- 83/158份真六点标签的6个点全部集中在归一化图像左上20%，包括唯一train样本和唯一val样本；这是下一步需视觉核实的主要系统性异常。
- 158份六点的三组L/R关系完全一致，但它们与当前导出集使用的画面x方向规则相反；需核对是影像水平翻转约定不同，还是关键点顺序/语义不同，不能先自动判错。
- `datasets/pose_det_data` 有158份合法标签，每份6个类别唯一的检测框，坐标全部合法；它们与158份真六点Pose标签逐点精确符合现有Pose→Detection转换逻辑。Detection数据不是独立真值。
- Pose图像未发现SHA-256完全重复组或跨split精确泄漏；但当前train/val各只1张，无法用于可靠训练和验证。
- 视觉抽查唯一train图 `4322078009__CR_TSPINE_20210202_slice0000.png`：原图是覆盖锁骨至骨盆的完整站立位脊柱X光，而其标签的6点均落在x约0.04–0.10、y约0.03–0.11的左上局部。这不是影像裁剪造成，高度疑似用了错误的图像宽高/缩放尺度归一化。
- 对照抽查的正常六点图是同类完整站立位影像，其标签x分布约0.30–0.77、y约0.34–0.74，与锁骨、髂骨和骶骨的解剖范围符合。因此83份左上聚集标签应作为一组系统性坐标错误候选，不应进入训练或验证。
- `docs/model_algorithm_report.md` 记录历史 `pose_data` 应为1565张（train=1252、val=157、test=156），而当前目录只有233张（train=1、val=1、test=231）。当前数据不是历史完整训练集，因此本轮只能全量审计“当前磁盘副本”，不能把结果外推为1565张历史样本的完整状况。
- `datasets/pose_data` 和 `datasets/pose_det_data` 未被Git跟踪，其文件没有可用的Git历史恢复路径。需继续在其他工程目录或数据盘查找完整旧数据副本。
- 已在 `/Users/liruirui/Documents/code/spine` 和 `/Volumes/E/spine_data` 按 `pose_data`、`pose_det_data`与六点相关目录名搜索，未找到第二份完整旧数据副本。
- 仓库现有脚本中没有找到这83份左上聚集标签的原始生成/转换脚本，因此暂时不能从代码直接追溯错误系数。可以通过坐标分布、图像尺寸和历史分析JSON进一步推断。
- 83份左上聚集标签构成一个统一批次：文件名全部为 `数字__CR_TSPINE_日期_slice0000`，498个点的visibility全为2，图像宽度中位数3012px、高度中位数5376px。另75份正常六点的450个点visibility全为1，图像宽度中位数1536px、高度中位数3488px。两组显然来自不同标注/转换链路。
- 异常组与正常组各关键点坐标中位数的比值并不恒定：x方向约6.8–7.8倍，y方向锁骨点约9.4–9.5倍、髂骨/骶骨点约6.35倍。将异常坐标统一乘8虽然83/83份仍在 `[0,1]`，但这不能证明解剖位置正确；乘10后只有3/83份全部点不越界。不存在有证据的单一缩放系数，不应自动放大修复。
- 当前158份真六点全部可见，与2025-12-03历史分析记录的1565份数据“56.17%可见、43.83%不可见”完全不同，再次证明当前目录只是经过挑选/重组的局部副本。
- 75份误混入标签已与 `datasets/pose_corner_data` 逐文件核对：75/75图像均存在且SHA-256一致，75/75标签均存在且字节一致。这是可确认的数据集任务混入，应从六点数据范围排除，但文件本身可能仍是有效椎体训练样本。
- 排除83份左上聚集后，当前副本只剩75份结构上正常的六点样本，且全部在test。初检两条“bbox未包住点”实际只超出 `0.000001`，是YOLO六位小数写出的边界舍入误差，不是真实bbox错误；审计容差已放宽到 `1e-5`。
- 256位dHash扫描在233张Pose目录图中发现7对距离≤1的强近重复；进一步统一缩放与强度归一化后，7对的像素相关系数均为0.999996–0.999998，可确认为同图重编码/重命名副本。
- 这7对每对都由“一份误混入的椎体四角样本”和“一份左上聚集错误六点样本”组成，且均在test内。它们不是跨split泄漏，但证明当前test目录同时存在同图的不同编码/命名与不同任务标签，不是干净的六点测试集。
- 158份真六点标签中，三组坐标关系100%为 `CR<CL`、`IR<IL`、`SR<SL`（画面x方向）。与新导出集重合的 `4322078009...` 检查在新标注中三组关系全部相反；旧图和新图因重编码字节哈希不同，但先前的图像指纹已确认为同一影像内容。这证明新旧数据的L/R语义或水平方向规则存在真实冲突，合并前必须统一。
- `train_pose.py` 配置了 `fliplr=0.5`，但 `6-train_ap_model/pose_data.yaml` 和数据目录内YAML都没有 `flip_idx`。当前Ultralytics 8.3.183会在Pose任务中因缺少 `flip_idx` 而直接将 `fliplr`和`flipud`设为0，所以六点训练中声明的50%水平翻转实际未生效。在加上`flip_idx`前，需先明确翻转后CR/CL等标签是保持解剖身份还是互换画面语义。

## 恢复后完整六点数据集复核（进行中）

- 恢复后的 `datasets/pose_data` 有2308对图像/标签：train=1846、val=231、test=231。标签由1565份六点Pose和743份椎体四角Pose组成，说明整个 `pose_corner_data` 规模的数据被混进了六点目录。
- 1565份六点标签中有879份的全部可见点都位于归一化左上20%范围；这一异常不只是此前233份残缺副本中的83份，而是完整旧六点集的系统性问题候选。
- 六点标签合计9390个点，visibility=1有4116个、visibility=2有5274个；自动检查暂未发现坐标越界或bbox没有包住点。
- `pose_det_data` 仍只有158份Detection标签，158份都与对应六点Pose按现有转换逻辑精确一致；另外1407份六点Pose没有Detection派生标签，因此Detection目录不是完整旧集，也不是独立真值。
- 图像SHA-256发现7个精确重复组，其中3组跨train/val/test，存在评估泄漏风险；仍需逐组比较标签是否一致。
- 分区组成精确对应“历史六点集 + 整套椎体集”：六点为1252/157/156，四角为594/74/75；743份混入数据与 `pose_corner_data` 的图像和标签逐字节全部一致。
- 879份左上错误与686份其余六点形成完全分离的两批：前者5274个点visibility全为2且所有坐标不超过0.15，后者4116个点visibility全为1且每份最大点坐标都大于0.5。多分区可视化确认前者原图完整、标签却被压在左上角。
- 剩余686份中发现3份明确结构异常：1份CR/CL互换，2份IR/IL与SR/SL点组疑似互换；扣除后683份通过自动结构检查（train533、val76、test74）。
- 7个精确重复图像组全部在左上错误批次，6组标签不一致，3组跨split；隔离879份时会一并消除这些已知精确泄漏。
- 2026-08-12复跑确认上述数字没有变化：审计器把879份判为`all_visible_points_top_left`，且这批所有点实际都不超过归一化坐标0.15；异常/正常叠加图再次证明异常图像并未裁掉躯干或骨盆，不能用构图解释。结论维持为整批坐标转换错误，禁止原样进入训练。

## 用户清理后的六点数据复核（2026-08-12）

- 清理后`datasets/pose_data`为686对：train 534、val 77、test 75，三个分区图像和标签数量及文件名均配对。
- 686份全部是单行23列的合法六点Pose标签；椎体四角混入为0、左上系统性聚集为0、坐标越界为0、bbox告警为0。
- 683份通过自动结构规则；仍有原先同样的3份明确结构异常：train一份CR/CL互换，val与test各一份IR/IL和SR/SL点组疑似互换。
- 当前精确重复图像组为0，跨split精确重复为0；之前左上错误批次中的7个重复组已随清理消失。
- `pose_det_data`未同步清理：158份Detection中75份仍能匹配当前Pose，83份对应已从Pose移除的旧左上错误样本。它不影响`pose_data.yaml`的Pose训练，但不应原样用于Detection训练或评估。
- 全量图像解码另发现train中的`1.2.156.147522.44.410947.1012.1.1.20250526160803.png`损坏：PNG IDAT CRC错误，Pillow完整加载和OpenCV `imread`均失败。仓库`datasets/`及E盘`spine_data`未找到同名完好副本；修复或重取原图前应将该图及标签排除出训练。
- 对其余685张可解码图像计算256位dHash，未发现距离不超过4的强近重复候选。排除3份结构异常和1张损坏图后，当前可正常解码且通过自动结构规则的候选为682份：train 532、val 76、test 74。

## 284组重复标注人工核对包（进行中）

- 用户所说284组对应移除532前的有标注快照：1567图+1567 JSON、284个精确重复组、569张组内图、冗余285张，284组均为`coordinates_or_structure`冲突。
- 当前 `training_export_normalized` 已移除错误532，因此只有1566对和283组；历史审计文件保存在 `修正备份_20260811_越界点复核/training_export_normalized_labeled_only_audit_before_532.json`。
- 第284组可通过当前规范化目录中的正确副本355，加上恢复备份中的532图像与JSON完整重建；核对包只读取两处来源，不恢复或修改现行数据集。
- 审计组通常为两张图，但总计569张说明存在一个三成员组；页面和结果格式需支持每组2个以上候选，用户要求的图1/图2/都不对应双成员组，三成员组应显示图1/图2/图3/都不对，避免丢失成员。
